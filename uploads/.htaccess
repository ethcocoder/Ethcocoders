# Prevent direct access to uploaded files
# Files should be accessed through the application only

# Deny direct access
<FilesMatch ".*">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Allow PHP scripts to access files
<FilesMatch "\.php$">
    Order Allow,Deny
    Allow from all
</FilesMatch>

